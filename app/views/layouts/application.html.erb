<!DOCTYPE html>
  <head>
    <title>Ecom</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>


    <% if session[:current_user_name] == "" %>
    <% @currentuser = "Guest" %>
    <% else %>
    <% @currentuser = session[:current_user_name] %>
    <% end %>
  </head>

  <body>
  <nav class="navbar navbar-light bg-light">
  <a class="navbar-brand" href="/">Home</a>
  <a class="navbar-brand" href="/products/">Products</a>
  <a class="navbar-brand" href="/"><%= @currentuser %></a>
  <strong>Welcome, <%= @currentuser %></strong>
  <form class="form-inline">
    <a class="btn btn-outline-primary"  href= "/login" type="button">Login</a>
    <div style="padding-left: 10px;">
    <a class="btn btn-outline-success" href = "/" type="button">Sign up</a>
    </div>
</form>
</nav>
    <%= yield %>
  </body>
</html>

